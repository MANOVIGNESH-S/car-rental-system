/**
 * Note: This component is not currently used in the application.
 * Routing and layout orchestration are handled directly in src/app/routes/index.tsx
 * using Data Router (RouterProvider).
 */
export const App = () => {
  return null;
};

export default App;